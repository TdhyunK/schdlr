import axios from 'axios';

export const NUM_FORMS = "NUM_FORMS"; 

export function numForms( numOfForms ){
 //for loop up to numOfForms to generate redux forms   
    console.log("num forms triggered");
}
